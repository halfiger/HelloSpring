🚀 Тепер ти вже майже готовий до:
Relations

Наприклад:

@OneToMany
@ManyToOne
І це буде ВЕЛИКИЙ стрибок

Бо там почнуться:

foreign keys
joins
lazy loading
cascade
fetch types
🔥 Але перед relations я б дав ще одну дуже корисну CRUD-тему
Наступне завдання

Реалізуй:

findByEmail(String email)
💡 Підказка
FROM User u WHERE u.email = :email
⚠️ І тут буде нова штука
getSingleResult()

або:

uniqueResult()
І ти вперше побачиш:
що робити, якщо знайдено 0 або 2 записи

А це вже дуже реальна backend-проблема 🙂